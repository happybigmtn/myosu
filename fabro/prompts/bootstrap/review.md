Read the workflow goal, the current lane `spec.md`, and the referenced repo
surfaces again.

Perform a final review pass on both curated artifacts:

- ensure the `spec.md` and `review.md` are consistent with each other
- remove vague statements and replace them with file-specific claims
- make the keep/reopen/reset judgment explicit
- ensure the artifacts are useful to a stateless Fabro run and to Raspberry's
  lane control plane

If needed, edit both files before finishing.
