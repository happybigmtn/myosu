The previous bootstrap lane run did not produce acceptable curated artifacts.

Read the workflow goal, inspect the existing `spec.md` and `review.md`, and fix
the specific issues that blocked verification. Keep the lane scope narrow and
do not spill into unrelated repo work.
