Read these inputs before you write anything:

- `outputs/chain/runtime/spec.md`
- `crates/myosu-chain/runtime/`
- `crates/myosu-chain/node/`
- `crates/myosu-chain/common/`

Your task is to produce or replace `outputs/chain/runtime/review.md`.

The `review.md` must answer:

- what runtime-related code is worth salvaging
- what must be restarted from scratch
- what exact manifests, crates, and proofs are missing
- what the very next restart phase should do

Keep the review tied to actual files in this repository.
