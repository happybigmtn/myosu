Read these inputs before you write anything:

- the lane's `spec.md`
- the doctrine, outputs, and ops surfaces named in the workflow goal

Your task is to produce or replace the lane's `review.md`.

The `review.md` must answer:

- what recurring oversight surfaces already exist and are worth keeping
- what is stale, misleading, or still shaped by Malinka-era assumptions
- what the first proof and operator signals should be
- what the next recurring implementation slice should do

Keep the review tied to actual files and current repository truth.
