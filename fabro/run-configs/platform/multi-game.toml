version = 1
graph = "../../workflows/bootstrap/multi-game.fabro"
goal = """
Bootstrap the Myosu `games:multi-game` lane for Fabro and Raspberry.

Scope:
- inspect `specsarchive/031626-06-multi-game-architecture.md`
- inspect `specsarchive/031626-16-cross-game-scoring.md`
- inspect `specsarchive/031626-17-spectator-protocol.md`
- inspect `outputs/games/poker-engine/` if it exists

Required curated artifacts:
- `outputs/games/multi-game/spec.md`
- `outputs/games/multi-game/review.md`

The `spec.md` must define:
- the purpose of the multi-game lane
- how Liar's Dice proof, expansion architecture, and cross-game scoring relate
- the smallest honest next implementation slices

The `review.md` must:
- make the keep/reopen/reset judgment explicit for the lane
- identify proof expectations and remaining blockers
- state whether the lane is ready for an implementation-family workflow next
"""
directory = "../../.."

[sandbox]
provider = "local"

[sandbox.local]
worktree_mode = "clean"
