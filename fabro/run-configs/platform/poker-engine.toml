version = 1
graph = "../../workflows/bootstrap/poker-engine.fabro"
goal = """
Bootstrap the Myosu `games:poker-engine` lane for Fabro and Raspberry.

Scope:
- inspect `specsarchive/031626-02b-poker-engine.md`
- inspect `specsarchive/031626-14-poker-variant-family.md`
- inspect the current `crates/myosu-games/` surfaces
- inspect `outputs/games/traits/` if it exists

Required curated artifacts:
- `outputs/games/poker-engine/spec.md`
- `outputs/games/poker-engine/review.md`

The `spec.md` must define:
- the platform-facing purpose of the poker-engine lane
- how solver wrapper, query, wire, and exploitability surfaces fit together
- the smallest honest next implementation slices

The `review.md` must:
- make the keep/reopen/reset judgment explicit for the lane
- identify proof expectations and remaining blockers
- state whether the lane is ready for an implementation-family workflow next
"""
directory = "../.."

[sandbox]
provider = "local"

[sandbox.local]
worktree_mode = "clean"
