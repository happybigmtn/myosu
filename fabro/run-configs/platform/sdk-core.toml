version = 1
graph = "../../workflows/bootstrap/sdk-core.fabro"
goal = """
Bootstrap the Myosu `sdk:core` lane for Fabro and Raspberry.

Scope:
- inspect `specsarchive/031626-19-game-engine-sdk.md`
- inspect `specsarchive/031626-02a-game-engine-traits.md`
- inspect `outputs/games/traits/` and `outputs/games/multi-game/` if they exist

Required curated artifacts:
- `outputs/sdk/core/spec.md`
- `outputs/sdk/core/review.md`

The `spec.md` must define:
- the purpose of the SDK core lane
- how scaffold, compliance tests, and registration flow fit together
- the smallest honest next implementation slices

The `review.md` must:
- make the keep/reopen/reset judgment explicit for the lane
- identify proof expectations and remaining blockers
- state whether the lane is ready for an implementation-family workflow next
"""
directory = "../.."

[sandbox]
provider = "local"

[sandbox.local]
worktree_mode = "clean"
