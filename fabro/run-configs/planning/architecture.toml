version = 1
graph = "../../workflows/planning/workflow.fabro"
goal = """
Author or refresh an implementation-ready architecture plan.
"""
directory = "../../.."

[sandbox]
provider = "local"

[sandbox.local]
worktree_mode = "clean"
