version = 1
graph = "../../workflows/bootstrap/chain-pallet-restart.fabro"
goal = """
Bootstrap the Myosu `chain:pallet` restart lane for Fabro and Raspberry.

Scope:
- inspect `crates/myosu-chain/pallets/game-solver/`
- treat the current pallet transplant as untrusted because the workspace build
  fails on it
- define the smallest honest restart slice that would reduce the pallet to a
  buildable Myosu-specific core

Required curated artifacts:
- `outputs/chain/pallet/spec.md`
- `outputs/chain/pallet/review.md`

The spec must define the restart boundary, proof shape, and next implementation
slices. The review must justify the restart and list only salvageable inputs.
"""
directory = "../../.."

[sandbox]
provider = "local"

[sandbox.local]
worktree_mode = "clean"
