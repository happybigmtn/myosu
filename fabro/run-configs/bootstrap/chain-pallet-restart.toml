version = 1
graph = "../../workflows/bootstrap/bootstrap.fabro"
goal = """
Verify the bootstrap artifacts for chain:pallet against
crates/myosu-chain/pallets/game-solver.

Required durable artifacts:
- outputs/chain/pallet/spec.md
- outputs/chain/pallet/review.md
"""
directory = "../../.."

[sandbox]
provider = "local"

[sandbox.local]
worktree_mode = "clean"
