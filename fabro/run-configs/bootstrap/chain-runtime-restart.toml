version = 1
graph = "../../workflows/bootstrap/bootstrap.fabro"
goal = """
Verify the bootstrap artifacts for chain:runtime against crates/myosu-chain/runtime.

Required durable artifacts:
- outputs/chain/runtime/spec.md
- outputs/chain/runtime/review.md
"""
directory = "../../.."

[sandbox]
provider = "local"

[sandbox.local]
worktree_mode = "clean"
