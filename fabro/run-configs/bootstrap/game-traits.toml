version = 1
graph = "../../workflows/bootstrap/bootstrap.fabro"
goal = """
Verify the bootstrap artifacts for games:traits.

Required durable artifacts:
- outputs/games/traits/spec.md
- outputs/games/traits/review.md
"""
directory = "../../.."

[sandbox]
provider = "local"

[sandbox.local]
worktree_mode = "clean"
