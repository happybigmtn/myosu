version = 1
graph = "../../workflows/bootstrap/bootstrap.fabro"
goal = """
Verify the bootstrap artifacts for tui:shell against crates/myosu-tui.

Required durable artifacts:
- outputs/tui/shell/spec.md
- outputs/tui/shell/review.md
"""
directory = "../../.."

[sandbox]
provider = "local"

[sandbox.local]
worktree_mode = "clean"
