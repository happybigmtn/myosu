version = 1
graph = "../../workflows/bootstrap/tui-shell.fabro"
goal = """
Bootstrap the Myosu `tui:shell` lane for Fabro and Raspberry.

Scope:
- inspect `crates/myosu-tui/`
- treat the shell, renderer, screen, input, and schema work as trusted leaf
  surfaces unless proof shows otherwise
- call out where proof claims currently exceed the tests

Required curated artifacts:
- `outputs/tui/shell/spec.md`
- `outputs/tui/shell/review.md`

The spec must define the lane boundary, proof shape, and next implementation
slices. The review must make the keep/reopen/reset judgment explicit.
"""
directory = "../../.."

[sandbox]
provider = "local"

[sandbox.local]
worktree_mode = "clean"
