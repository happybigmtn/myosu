version = 1
graph = "../../workflows/maintenance/workflow.fabro"
goal = """
Refresh the strategy planning recurring report.
"""
directory = "../../.."

[sandbox]
provider = "local"

[sandbox.local]
worktree_mode = "clean"
