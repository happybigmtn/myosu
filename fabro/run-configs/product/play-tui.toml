version = 1
graph = "../../workflows/bootstrap/play-tui.fabro"
goal = """
Bootstrap the Myosu `play:tui` lane for Fabro and Raspberry.

Scope:
- inspect `specsarchive/031626-05-gameplay-cli.md`
- inspect `specsarchive/031626-07-tui-implementation.md`
- inspect the current `crates/myosu-tui/` surfaces
- inspect `outputs/games/traits/` and `outputs/tui/shell/` if they exist

Required curated artifacts:
- `outputs/play/tui/spec.md`
- `outputs/play/tui/review.md`

The `spec.md` must define:
- the product-facing purpose of the TUI gameplay lane
- the dependency boundary between game traits, shell, poker renderer, training
  mode, and future chain-backed play
- the smallest honest next implementation slices

The `review.md` must:
- make the keep/reopen/reset judgment explicit for the lane
- identify proof expectations and remaining blockers
- state whether the lane is ready for an implementation-family workflow next
"""
directory = "../../.."

[sandbox]
provider = "local"

[sandbox.local]
worktree_mode = "clean"
