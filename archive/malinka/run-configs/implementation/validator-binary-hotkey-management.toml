version = 1
graph = "../../workflows/implementation/validator-binary-hotkey-management.fabro"
goal = """
Implement hotkey management

Child work item of plan: Validator Binary Implementation

Objective:
Load and manage validator hotkey for signing registration and weight submission transactions

Owned surfaces:
- `crates/myosu-validator/src/hotkey.rs`

Proof commands:
- `cargo build -p myosu-validator`
- `cargo test -p myosu-validator -- hotkey`

Required durable artifacts:
- `spec.md`
- `review.md`
"""
directory = "../../.."

[llm]
provider = "anthropic"
model = "MiniMax-M2.7-highspeed"

[llm.fallbacks]
anthropic = ["openai", "gemini", "kimi", "minimax"]

[sandbox]
provider = "local"

[sandbox.local]
worktree_mode = "always"

[sandbox.env]
FABRO_STRICT_PROVIDER = "1"

[integration]
enabled = true
strategy = "squash"
target_branch = "origin/HEAD"
artifact_path = "../../../integration.md"
