# Implement all poker screens Lane — Plan

Lane: `tui-full-implementation-implement-poker-screens`

Goal:
- Implement all poker screens

Child work item of plan: TUI Full Implementation

Objective:
Tab navigation between all five poker screens works without panics

Owned surfaces:
- `crates/myosu-tui/src/screens.rs`
- `crates/myosu-games-poker/src/renderer.rs`

Proof commands:
- `cargo build -p myosu-tui`
- `cargo test -p myosu-tui`

Required durable artifacts:
- `spec.md`
- `review.md`


Verification artifact must cover
- summarize the automated proof commands that ran and their outcomes

Stage ownership:
- do not write `promotion.md` during Plan/Implement
- do not hand-author `quality.md`; it is regenerated by the Quality Gate
- `promotion.md` is owned by the Review stage only
- keep source edits inside the named slice and touched surfaces
