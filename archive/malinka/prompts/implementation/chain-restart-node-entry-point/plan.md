# Create myosu-chain/node binary entry point Lane — Plan

Lane: `chain-restart-node-entry-point`

Goal:
- Create myosu-chain/node binary entry point

Child work item of plan: Chain Restart

Objective:
Create main.rs and service.rs so the chain has a runnable binary entry point

Owned surfaces:
- `crates/myosu-chain/node/src/main.rs`
- `crates/myosu-chain/node/src/service.rs`

Proof commands:
- `cargo check -p myosu-chain-node`

Required durable artifacts:
- `spec.md`
- `review.md`


Verification artifact must cover
- summarize the automated proof commands that ran and their outcomes

Stage ownership:
- do not write `promotion.md` during Plan/Implement
- do not hand-author `quality.md`; it is regenerated by the Quality Gate
- `promotion.md` is owned by the Review stage only
- keep source edits inside the named slice and touched surfaces
