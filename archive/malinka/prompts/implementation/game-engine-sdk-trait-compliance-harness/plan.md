# Implement trait compliance test harness Lane — Plan

Lane: `game-engine-sdk-trait-compliance-harness`

Goal:
- Implement trait compliance test harness

Child work item of plan: Game Engine SDK Development

Objective:
Run trait compliance checks (info_set_count, serialize, deserialize) and report pass/fail per AC

Owned surfaces:
- `crates/myosu-sdk/src/harness.rs`

Proof commands:
- `cargo build -p myosu-sdk`
- `myosu-sdk test --crate /tmp/new-game`

Required durable artifacts:
- `spec.md`
- `review.md`


Verification artifact must cover
- summarize the automated proof commands that ran and their outcomes

Stage ownership:
- do not write `promotion.md` during Plan/Implement
- do not hand-author `quality.md`; it is regenerated by the Quality Gate
- `promotion.md` is owned by the Review stage only
- keep source edits inside the named slice and touched surfaces
