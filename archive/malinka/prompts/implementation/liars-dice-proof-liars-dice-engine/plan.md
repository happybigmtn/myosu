# Implement Liar's Dice game engine Lane — Plan

Lane: `liars-dice-proof-liars-dice-engine`

Goal:
- Implement Liar's Dice game engine

Child work item of plan: Liar's Dice Architecture Proof

Objective:
Implement CfrGame trait for Liar's Dice with exactly 147,420 terminal states using Copy workaround for variable-length bid history

Owned surfaces:
- `crates/myosu-games-liars-dice/`

Proof commands:
- `cargo test -p myosu-games-liars-dice -- terminal_state_count`

Required durable artifacts:
- `spec.md`
- `review.md`


Verification artifact must cover
- summarize the automated proof commands that ran and their outcomes

Stage ownership:
- do not write `promotion.md` during Plan/Implement
- do not hand-author `quality.md`; it is regenerated by the Quality Gate
- `promotion.md` is owned by the Review stage only
- keep source edits inside the named slice and touched surfaces
