# Add shell state tests to myosu-tui Lane — Plan

Lane: `test-coverage-sprint-tui-shell-state-tests`

Goal:
- Add shell state tests to myosu-tui

Child work item of plan: Test Coverage Sprint

Objective:
Add tests verifying that shell state updates correctly in response to events

Owned surfaces:
- `crates/myosu-tui/src/tests/shell_state_tests.rs`
- `crates/myosu-tui/src/shell.rs`

Proof commands:
- `cargo test -p myosu-tui -- shell_state`

Required durable artifacts:
- `spec.md`
- `review.md`


Verification artifact must cover
- summarize the automated proof commands that ran and their outcomes

Stage ownership:
- do not write `promotion.md` during Plan/Implement
- do not hand-author `quality.md`; it is regenerated by the Quality Gate
- `promotion.md` is owned by the Review stage only
- keep source edits inside the named slice and touched surfaces
