# Complete implement/ workflow family Lane — Plan

Lane: `fabro-workflow-library-implement-family`

Goal:
- Complete implement/ workflow family

Child work item of plan: Complete Fabro Workflow Library

Objective:
Add spec-implement-verify-promote workflow with documented phase contracts

Owned surfaces:
- `fabro/workflows/implement/`
- `fabro/run-configs/implement/`

Proof commands:
- `test -f fabro/workflows/implement/workflow.fabro`
- `test -f fabro/run-configs/implement/game-traits.toml`

Required durable artifacts:
- `spec.md`
- `review.md`


Verification artifact must cover
- summarize the automated proof commands that ran and their outcomes

Stage ownership:
- do not write `promotion.md` during Plan/Implement
- do not hand-author `quality.md`; it is regenerated by the Quality Gate
- `promotion.md` is owned by the Review stage only
- keep source edits inside the named slice and touched surfaces
