# Write devnet launch runbook Lane — Plan

Lane: `operational-setup-devnet-launch-runbook`

Goal:
- Write devnet launch runbook

Child work item of plan: Operational Setup

Objective:
Create step-by-step runbook that takes an engineer from zero to a working devnet with participants

Owned surfaces:
- `ops/runbooks/devnet-launch.md`

Proof commands:
- `test -f ops/runbooks/devnet-launch.md`

Required durable artifacts:
- `spec.md`
- `review.md`


Verification artifact must cover
- summarize the automated proof commands that ran and their outcomes

Stage ownership:
- do not write `promotion.md` during Plan/Implement
- do not hand-author `quality.md`; it is regenerated by the Quality Gate
- `promotion.md` is owned by the Review stage only
- keep source edits inside the named slice and touched surfaces
