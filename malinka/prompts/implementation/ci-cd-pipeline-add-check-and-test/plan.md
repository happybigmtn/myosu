# Add cargo check and cargo test for active crates Lane — Plan

Lane: `ci-cd-pipeline-add-check-and-test`

Goal:
- Add cargo check and cargo test for active crates

Child work item of plan: CI/CD Pipeline Setup

Objective:
Add CI steps that run cargo check and cargo test against myosu-games and myosu-tui crates

Owned surfaces:
- `.github/workflows/ci.yml`

Proof commands:
- `cargo check -p myosu-games -p myosu-tui`
- `cargo test -p myosu-games -p myosu-tui`

Required durable artifacts:
- `spec.md`
- `review.md`


Verification artifact must cover
- summarize the automated proof commands that ran and their outcomes

Stage ownership:
- do not write `promotion.md` during Plan/Implement
- do not hand-author `quality.md`; it is regenerated by the Quality Gate
- `promotion.md` is owned by the Review stage only
- keep source edits inside the named slice and touched surfaces
