# Implement MCCFR game-solver using myosu-games Lane — Plan

Lane: `miner-binary-mccfr-solver`

Goal:
- Implement MCCFR game-solver using myosu-games

Child work item of plan: Miner Binary Implementation

Objective:
Run MCCFR solving for the configured game via myosu-games and produce strategy checkpoints

Owned surfaces:
- `crates/myosu-miner/src/main.rs`

Proof commands:
- `cargo build -p myosu-miner`
- `cargo run -p myosu-miner -- --subnet 1 --solve --iterations 10000`

Required durable artifacts:
- `spec.md`
- `review.md`


Verification artifact must cover
- summarize the automated proof commands that ran and their outcomes

Stage ownership:
- do not write `promotion.md` during Plan/Implement
- do not hand-author `quality.md`; it is regenerated by the Quality Gate
- `promotion.md` is owned by the Review stage only
- keep source edits inside the named slice and touched surfaces
