# Implement Encoder and GameRenderer Lane — Plan

Lane: `liars-dice-proof-encoder-renderer`

Goal:
- Implement Encoder and GameRenderer

Child work item of plan: Liar's Dice Architecture Proof

Objective:
Implement bincode serialization roundtrip and TUI rendering for Liar's Dice game state

Owned surfaces:
- `crates/myosu-games-liars-dice/src/`

Proof commands:
- `cargo test -p myosu-games-liars-dice -- roundtrip`

Required durable artifacts:
- `spec.md`
- `review.md`


Verification artifact must cover
- summarize the automated proof commands that ran and their outcomes

Stage ownership:
- do not write `promotion.md` during Plan/Implement
- do not hand-author `quality.md`; it is regenerated by the Quality Gate
- `promotion.md` is owned by the Review stage only
- keep source edits inside the named slice and touched surfaces
