# Add property-based round-trip tests to myosu-games Lane — Plan

Lane: `test-coverage-sprint-games-roundtrip-tests`

Goal:
- Add property-based round-trip tests to myosu-games

Child work item of plan: Test Coverage Sprint

Objective:
Add proptest-based tests verifying GameType::from_bytes(g.to_bytes()) == g for random configurations

Owned surfaces:
- `crates/myosu-games/Cargo.toml`
- `crates/myosu-games/src/tests/`
- `crates/myosu-games/src/traits.rs`

Proof commands:
- `cargo test -p myosu-games -- serialization_roundtrip`

Required durable artifacts:
- `spec.md`
- `review.md`


Verification artifact must cover
- summarize the automated proof commands that ran and their outcomes

Stage ownership:
- do not write `promotion.md` during Plan/Implement
- do not hand-author `quality.md`; it is regenerated by the Quality Gate
- `promotion.md` is owned by the Review stage only
- keep source edits inside the named slice and touched surfaces
