# Add smoke test to all generated implementation programs Lane — Plan

Lane: `fabro-quality-hardening-smoke-test-binaries`

Goal:
- Add smoke test to all generated implementation programs

Child work item of plan: Fabro Quality Hardening

Objective:
Add smoke test invocations that verify binaries produce expected behavior beyond just exiting with code 0

Owned surfaces:
- `fabro/run-configs/product/play-tui.toml`
- `fabro/run-configs/platform/poker-engine.toml`
- `fabro/run-configs/platform/multi-game.toml`
- `fabro/run-configs/platform/sdk-core.toml`

Proof commands:
- `cargo run -p myosu-play -- --smoke-test`

Required durable artifacts:
- `spec.md`
- `review.md`


Verification artifact must cover
- summarize the automated proof commands that ran and their outcomes

Stage ownership:
- do not write `promotion.md` during Plan/Implement
- do not hand-author `quality.md`; it is regenerated by the Quality Gate
- `promotion.md` is owned by the Review stage only
- keep source edits inside the named slice and touched surfaces
