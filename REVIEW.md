# REVIEW

Open items from the 2026-04-02 review pass. The duplicate entries that had
accidentally inflated this queue were removed during review; only not-yet-cleared
items remain here.

- `RT-001` commit `baa9c11951259ee4f2b96dd6fb1f983321ff65eb`; status: still open pending direct runtime and local-loop revalidation. Validation to clear: `SKIP_WASM_BUILD=1 cargo check -p myosu-chain-runtime`; `cargo test -p pallet-game-solver stage_0_flow --quiet`; `cargo build -p myosu-chain-runtime`; `SKIP_WASM_BUILD=1 cargo test -p myosu-chain --test stage0_local_loop --quiet`
- `RT-002` commit `a1f70c04106bf818b685b33b57a4f317033761bb`; status: still open pending direct node/runtime proof rerun. The dependency-tree grep already returns no Frontier/EVM matches, but the full node proof has not been cleared in this pass. Validation to clear: `cargo check -p pallet-admin-utils --quiet`; `SKIP_WASM_BUILD=1 cargo check -p myosu-chain --quiet`; `cargo tree -p myosu-chain --prefix none | rg '^(fc-|fp-|pallet-evm|pallet-ethereum)'`; `SKIP_WASM_BUILD=1 cargo test -p myosu-chain --test stage0_local_loop --quiet`
- `RT-003` commit `550adc735e70767db56b5167669d6b47eac5c944`; status: still open pending direct pallet/miner/validator proof rerun. Validation to clear: `cargo test -p pallet-game-solver stage_0_flow --quiet`; `SKIP_WASM_BUILD=1 cargo test -p myosu-miner -p myosu-validator --quiet`
- `RT-004` commit `0a5273c76d735fc85e50df80218e64492765626c`; status: failed provenance review. The cited commit is `EM-001` (`myosu: EM-001 remove root stake weighting`), not a TODO/FIXME cleanup slice. The active-tree count check currently returns `16`, but this item is not trustworthy until the real cleanup commit is identified or a dedicated replacement slice lands. Validation to clear: `rg -n "TODO|FIXME" crates/myosu-chain/runtime crates/myosu-chain/node crates/myosu-chain/pallets/game-solver/src --glob '!**/migrations/**' --glob '!**/tests/**' -S | wc -l`; `SKIP_WASM_BUILD=1 cargo check -p myosu-chain-runtime`; `cargo test -p pallet-game-solver stage_0_flow --quiet`
- `EM-001` commit `0a5273c76d735fc85e50df80218e64492765626c`; status: still open pending direct coinbase/stage-0 proof rerun in this pass. Validation to clear: `cargo test -p pallet-game-solver coinbase --quiet`; `cargo test -p pallet-game-solver stage_0_flow --quiet`
- `EM-002` commit `9941b62a0342f8b10ab32446f47cb013e23d7faf`; status: still open pending direct determinism proof rerun. Validation to clear: `cargo test -p pallet-game-solver determinism --quiet`
- `SEC-001` commit `08f00a486a25a4bb16a2ae1e17e973eeb9ca92f1`; status: failed review. `cargo audit` with the current ignore list still reports `13 allowed warnings found`, including `RUSTSEC-2025-0141` on owned `bincode 1.3.3` usage plus `RUSTSEC-2024-0388`, `RUSTSEC-2025-0057`, `RUSTSEC-2024-0384`, `RUSTSEC-2020-0168`, `RUSTSEC-2022-0061`, `RUSTSEC-2024-0436`, `RUSTSEC-2024-0370`, `RUSTSEC-2025-0010`, `RUSTSEC-2021-0127`, `RUSTSEC-2026-0002`, and `RUSTSEC-2024-0442`. Validation to clear: a truthful `cargo audit` gate plus explicit treatment for the remaining warning-class advisories
- `IT-001` commit `98630d847fef0f50b82ca8695095a69bc9f0aa92`; status: still open pending direct devnet start/stop proof rerun. Validation to clear: `bash tests/e2e/helpers/start_devnet.sh && bash tests/e2e/helpers/wait_for_block.sh 1 && bash tests/e2e/helpers/stop_devnet.sh`
- `EM-003` commit `57266177d74d667193f4c48980868be02dfe20ef`; status: still open pending direct end-to-end emission proof rerun. Validation to clear: `bash tests/e2e/emission_flow.sh`
- `IT-002` commit `3ec60ef057e3ce95cd30778e6fdf2212109a094b`; status: still open pending direct local-loop proof rerun. Validation to clear: `bash tests/e2e/local_loop.sh`
- `IT-003` commit `1a77dac7623abb59b917b0c6eee0f6602c13da81`; status: still open pending direct validator determinism proof rerun. Validation to clear: `bash tests/e2e/validator_determinism.sh`
- `DN-001` commit `2dca010f350d85bfc7a0228c2fa519e1d8f0c54a`; status: still open pending direct devnet chain-spec proof rerun. Validation to clear: `cargo test -p myosu-chain base_devnet_spec_uses_custom_chain_type_and_non_dev_authorities --quiet`; `cargo test -p myosu-chain devnet_config_bootstraps_subnet_seven_in_storage --quiet`; `SKIP_WASM_BUILD=1 cargo run -p myosu-chain -- build-spec --chain devnet --raw > /dev/null`
- `DN-003` commit `2b571db3c704c013d9afc784bf427cd6bdfe740f`; status: still open pending direct two-node sync proof rerun. Validation to clear: `bash tests/e2e/two_node_sync.sh`; `SKIP_WASM_BUILD=1 cargo test -p myosu-chain --test stage0_local_loop --quiet`
