# REVIEW

Open items from the 2026-04-02 review pass. The duplicate entries that had
accidentally inflated this queue were removed during review; only not-yet-cleared
items remain here.

- `RT-001` commit `baa9c11951259ee4f2b96dd6fb1f983321ff65eb`; status: failed review. `cargo build -p myosu-chain-runtime` and `SKIP_WASM_BUILD=1 cargo check -p myosu-chain-runtime` both pass, but the reviewed slice still does not satisfy the current spec text: the default stage-0 runtime composes nine pallets, while `specs/040226-01-chain-runtime-reduction.md` still claims seven or fewer. The local-loop proof also was not rerun in this pass. Validation to clear: reconcile the runtime-reduction spec with the shipped runtime shape, then rerun `cargo test -p pallet-game-solver stage_0_flow --quiet`; `SKIP_WASM_BUILD=1 cargo test -p myosu-chain --test stage0_local_loop --quiet`
- `RT-002` commit `a1f70c04106bf818b685b33b57a4f317033761bb`; status: partially revalidated, still open. `cargo check -p pallet-admin-utils` passes and `cargo tree -p myosu-chain --prefix none | rg '^(fc-|fp-|pallet-evm|pallet-ethereum)'` returns no Frontier/EVM matches, but the full node/runtime proof was not rerun to completion in this pass. Validation to clear: `SKIP_WASM_BUILD=1 cargo check -p myosu-chain --quiet`; `SKIP_WASM_BUILD=1 cargo test -p myosu-chain --test stage0_local_loop --quiet`
- `RT-003` commit `550adc735e70767db56b5167669d6b47eac5c944`; status: failed review. The stage-0 dispatch surface now compiles removed extrinsics out of the call enum instead of returning an explicit unavailable dispatch error, so the implementation no longer matches the contract described in the spec/plan. Direct pallet/miner/validator reruns also remain outstanding. Validation to clear: either restore the explicit unavailable-call contract or narrow the spec/API claim, then rerun `cargo test -p pallet-game-solver stage_0_flow --quiet`; `SKIP_WASM_BUILD=1 cargo test -p myosu-miner -p myosu-validator --quiet`
- `RT-004` commit `0a5273c76d735fc85e50df80218e64492765626c`; status: failed provenance review. The cited commit is `EM-001` (`myosu: EM-001 remove root stake weighting`), not a TODO/FIXME cleanup slice. The active-tree count check currently returns `16`, and several surviving notes are still raw TODO/FIXME markers rather than justified deferments. Validation to clear: identify the real cleanup slice or land a replacement cleanup change, then rerun `rg -n "TODO|FIXME" crates/myosu-chain/runtime crates/myosu-chain/node crates/myosu-chain/pallets/game-solver/src --glob '!**/migrations/**' --glob '!**/tests/**' -S | wc -l`; `SKIP_WASM_BUILD=1 cargo check -p myosu-chain-runtime`; `cargo test -p pallet-game-solver stage_0_flow --quiet`
- `EM-001` commit `0a5273c76d735fc85e50df80218e64492765626c`; status: failed review. Root stake weighting was removed, but the live coinbase path still routes through swap/price-shaped helpers, so the implementation does not yet match the spec claim that stage-0 emission runs without root-network or AMM dependencies. Direct coinbase/stage-0 reruns also remain outstanding. Validation to clear: remove or explicitly justify the remaining swap/price-shaped coinbase path, then rerun `cargo test -p pallet-game-solver coinbase --quiet`; `cargo test -p pallet-game-solver stage_0_flow --quiet`
- `EM-002` commit `9941b62a0342f8b10ab32446f47cb013e23d7faf`; status: failed review. The current unit proof exercises repeated Yuma epoch execution, not cross-validator scoring determinism, so the evidence does not support the review claim as written. Validation to clear: add or point at proof that actually covers validator scoring determinism, then rerun `cargo test -p pallet-game-solver determinism --quiet` and `bash tests/e2e/validator_determinism.sh`
- `IT-001` commit `98630d847fef0f50b82ca8695095a69bc9f0aa92`; status: still open pending direct devnet start/stop proof rerun. Validation to clear: `bash tests/e2e/helpers/start_devnet.sh && bash tests/e2e/helpers/wait_for_block.sh 1 && bash tests/e2e/helpers/stop_devnet.sh`
- `EM-003` commit `57266177d74d667193f4c48980868be02dfe20ef`; status: still open pending direct end-to-end emission proof rerun. Validation to clear: `bash tests/e2e/emission_flow.sh`
- `IT-002` commit `3ec60ef057e3ce95cd30778e6fdf2212109a094b`; status: still open pending direct local-loop proof rerun. Validation to clear: `bash tests/e2e/local_loop.sh`
- `IT-003` commit `1a77dac7623abb59b917b0c6eee0f6602c13da81`; status: still open pending direct validator determinism proof rerun. Validation to clear: `bash tests/e2e/validator_determinism.sh`
- `DN-001` commit `2dca010f350d85bfc7a0228c2fa519e1d8f0c54a`; status: failed review. The devnet chain-spec tests cover authorities and subnet storage, but the chain-spec builder does not embed bootnodes and the current two-node sync proof injects `--bootnodes` externally at runtime. Validation to clear: embed truthful bootnodes in the devnet chain spec or narrow the claim to the operator-bundle patch path, then rerun `cargo test -p myosu-chain base_devnet_spec_uses_custom_chain_type_and_non_dev_authorities --quiet`; `cargo test -p myosu-chain devnet_config_bootstraps_subnet_seven_in_storage --quiet`; `SKIP_WASM_BUILD=1 cargo run -p myosu-chain -- build-spec --chain devnet --raw > /dev/null`
- `DN-003` commit `2b571db3c704c013d9afc784bf427cd6bdfe740f`; status: still open pending direct two-node sync proof rerun. `DN-001` must stay in mind here because the current proof path depends on manual bootnode injection. Validation to clear: `bash tests/e2e/two_node_sync.sh`; `SKIP_WASM_BUILD=1 cargo test -p myosu-chain --test stage0_local_loop --quiet`
