use crate::chain_spec::ChainSpec;

/// Builds the test finney chain spec.
pub fn finney_testnet_config() -> Result<ChainSpec, String> {
    Err("testnet chain spec is not restored yet".to_string())
}
