# COMPLETED

- 2026-04-05 `P-001` Reconciled the stale queue entry against the live repo and confirmed the former "uncommitted hardening batch" had already landed in `ba63a7d662aaee9877c94857f92d62c329875093` (`myosu: auto loop checkpoint`). Validation: repo evidence review across `.github/workflows/ci.yml`, `crates/myosu-chain/pallets/game-solver/src/coinbase/run_coinbase.rs`, `crates/myosu-validator/src/validation.rs`, `crates/myosu-games-poker/src/solver.rs`, and `git diff --stat`.
- 2026-04-05 `P-002` Quantified stage-0 coinbase truncation drift at <=2 rao per accrued block / 6 rao per default tempo-2 epoch, documented the bound at the `tou64!` conversion site, and tightened the stage-0 emission-accounting tolerance. Validation: `cargo test -p pallet-game-solver -- truncation`; `cargo test -p pallet-game-solver -- stage_0`. Commit: `9675dd9428bede39ad0065794d090a564421dd75`.
- 2026-04-05 `P-003` Wired `tests/e2e/emission_flow.sh` into the `integration-e2e` CI job, raised that job timeout to 15 minutes to match the measured proof cost, and recorded the new timing guidance in `AGENTS.md`. Validation: `/tmp/actionlint-bin/actionlint .github/workflows/ci.yml`; `bash tests/e2e/local_loop.sh`; `bash tests/e2e/validator_determinism.sh`; `bash tests/e2e/emission_flow.sh`. Commit: `99ea7cc1e6f09bbec37567718ce21051aa392daf`.
- 2026-04-05 `P-004` Pinned every mutable GitHub Action ref in `.github/workflows/ci.yml` to immutable commit SHAs, added read-only workflow permissions plus `persist-credentials: false` on checkout steps so `zizmor` no longer reports pinning, credential-persistence, or excessive-permissions findings, and recorded the remaining `dtolnay/rust-toolchain` advisory as follow-on work. Validation: `python` SHA-ref scanner over `.github/workflows/ci.yml`; `/tmp/actionlint-bin/actionlint .github/workflows/ci.yml`; `zizmor --min-severity medium .github/workflows/ci.yml`; `bash .github/scripts/check_stage0_repo_shape.sh`; `bash .github/scripts/check_doctrine_integrity.sh`; `bash .github/scripts/check_plan_quality.sh`. Commit: `e7f7944b2178215aa610e8a394675b1fdc70a45b`.
- 2026-04-05 `P-005` Wired `tests/e2e/two_node_sync.sh` into the `integration-e2e` CI job after `local_loop.sh`, removed the finished queue entry from `IMPLEMENTATION_PLAN.md`, recorded the measured four-proof timing in `AGENTS.md`, and logged the dangling `@RTK.md` reference as `DOC-OPS-001` in `WORKLIST.md`. Validation: `actionlint .github/workflows/ci.yml`; `zizmor --min-severity medium .github/workflows/ci.yml`; `bash tests/e2e/local_loop.sh`; `bash tests/e2e/two_node_sync.sh`; `bash tests/e2e/validator_determinism.sh`; `bash tests/e2e/emission_flow.sh`. Commit: `6e1556a081cac5df847e60b61e3937b12dd4a05c`.
- 2026-04-05 `P-005` follow-up: fixed `myosu-chain-runtime`'s build script so Cargo reruns it when `SKIP_WASM_BUILD`, `CARGO_TARGET_DIR`, or `PROFILE` change, which prevents cached targets from leaving `WASM_BINARY = None` and breaking `local_loop.sh` / `build-spec --chain devnet` in CI before the new `two_node_sync.sh` step can run. Validation: isolated repro in `/tmp/myosu-skipwasm-repro` (`SKIP_WASM_BUILD=1 cargo build -p myosu-chain --features fast-runtime`; `cargo build -p myosu-chain-runtime`; `SKIP_WASM_BUILD=1 cargo build -p myosu-chain --features fast-runtime`; `myosu-chain build-spec --chain devnet`); `bash tests/e2e/local_loop.sh`; `bash tests/e2e/two_node_sync.sh`; `bash tests/e2e/validator_determinism.sh`; `bash tests/e2e/emission_flow.sh`. Commit: `d44b522ccd0652cc173ff85ec8e0c8c7af89a205`.
