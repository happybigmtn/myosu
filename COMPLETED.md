# COMPLETED

- 2026-04-05 `P-001` Reconciled the stale queue entry against the live repo and confirmed the former "uncommitted hardening batch" had already landed in `ba63a7d662aaee9877c94857f92d62c329875093` (`myosu: auto loop checkpoint`). Validation: repo evidence review across `.github/workflows/ci.yml`, `crates/myosu-chain/pallets/game-solver/src/coinbase/run_coinbase.rs`, `crates/myosu-validator/src/validation.rs`, `crates/myosu-games-poker/src/solver.rs`, and `git diff --stat`.
- 2026-04-05 `P-002` Quantified stage-0 coinbase truncation drift at <=2 rao per accrued block / 6 rao per default tempo-2 epoch, documented the bound at the `tou64!` conversion site, and tightened the stage-0 emission-accounting tolerance. Validation: `cargo test -p pallet-game-solver -- truncation`; `cargo test -p pallet-game-solver -- stage_0`. Commit: `9675dd9428bede39ad0065794d090a564421dd75`.
- 2026-04-05 `P-003` Wired `tests/e2e/emission_flow.sh` into the `integration-e2e` CI job, raised that job timeout to 15 minutes to match the measured proof cost, and recorded the new timing guidance in `AGENTS.md`. Validation: `/tmp/actionlint-bin/actionlint .github/workflows/ci.yml`; `bash tests/e2e/local_loop.sh`; `bash tests/e2e/validator_determinism.sh`; `bash tests/e2e/emission_flow.sh`. Commit: `99ea7cc1e6f09bbec37567718ce21051aa392daf`.
- 2026-04-05 `P-004` Pinned every mutable GitHub Action ref in `.github/workflows/ci.yml` to immutable commit SHAs, added read-only workflow permissions plus `persist-credentials: false` on checkout steps so `zizmor` no longer reports pinning, credential-persistence, or excessive-permissions findings, and recorded the remaining `dtolnay/rust-toolchain` advisory as follow-on work. Validation: `python` SHA-ref scanner over `.github/workflows/ci.yml`; `/tmp/actionlint-bin/actionlint .github/workflows/ci.yml`; `zizmor --min-severity medium .github/workflows/ci.yml`; `bash .github/scripts/check_stage0_repo_shape.sh`; `bash .github/scripts/check_doctrine_integrity.sh`; `bash .github/scripts/check_plan_quality.sh`. Commit: `e7f7944b2178215aa610e8a394675b1fdc70a45b`.
- 2026-04-05 `P-005` Wired `tests/e2e/two_node_sync.sh` into the `integration-e2e` CI job after `local_loop.sh`, removed the finished queue entry from `IMPLEMENTATION_PLAN.md`, recorded the measured four-proof timing in `AGENTS.md`, and logged the dangling `@RTK.md` reference as `DOC-OPS-001` in `WORKLIST.md`. Validation: `actionlint .github/workflows/ci.yml`; `zizmor --min-severity medium .github/workflows/ci.yml`; `bash tests/e2e/local_loop.sh`; `bash tests/e2e/two_node_sync.sh`; `bash tests/e2e/validator_determinism.sh`; `bash tests/e2e/emission_flow.sh`. Commit: `6e1556a081cac5df847e60b61e3937b12dd4a05c`.
- 2026-04-05 `P-005` follow-up: fixed `myosu-chain-runtime`'s build script so Cargo reruns it when `SKIP_WASM_BUILD`, `CARGO_TARGET_DIR`, or `PROFILE` change, which prevents cached targets from leaving `WASM_BINARY = None` and breaking `local_loop.sh` / `build-spec --chain devnet` in CI before the new `two_node_sync.sh` step can run. Validation: isolated repro in `/tmp/myosu-skipwasm-repro` (`SKIP_WASM_BUILD=1 cargo build -p myosu-chain --features fast-runtime`; `cargo build -p myosu-chain-runtime`; `SKIP_WASM_BUILD=1 cargo build -p myosu-chain --features fast-runtime`; `myosu-chain build-spec --chain devnet`); `bash tests/e2e/local_loop.sh`; `bash tests/e2e/two_node_sync.sh`; `bash tests/e2e/validator_determinism.sh`; `bash tests/e2e/emission_flow.sh`. Commit: `d44b522ccd0652cc173ff85ec8e0c8c7af89a205`.
- 2026-04-05 `P-005` follow-up: cleared inherited `SKIP_WASM_BUILD` in the E2E runtime-cache refresh paths used by `tests/e2e/helpers/start_devnet.sh` and `tests/e2e/two_node_sync.sh`, which was the remaining CI-only root cause after the `build.rs` fix because the `integration-e2e` job itself exports `SKIP_WASM_BUILD=1`. Validation: `env SKIP_WASM_BUILD=1 bash tests/e2e/local_loop.sh`; `env SKIP_WASM_BUILD=1 bash tests/e2e/two_node_sync.sh`; `env SKIP_WASM_BUILD=1 bash tests/e2e/validator_determinism.sh`; `env SKIP_WASM_BUILD=1 bash tests/e2e/emission_flow.sh`. Commit: `382b5692b9fcc6b61c08828910a2d8dcc60adc61`.
- 2026-04-05 `P-005` follow-up: cleared the remaining CI blockers after the `two_node_sync` wiring landed by feature-gating `full-runtime`-only pallet support code so stage-0 clippy/tests stop compiling dead surfaces, removing the matching `needless_return` in `subtensor-transaction-fee`, and aligning the `cargo audit` workflow allowlist with the already-deferred `WORKLIST.md` `SEC-001` advisory set. Validation: `/tmp/actionlint-bin/actionlint .github/workflows/ci.yml`; `cargo audit -D warnings --ignore RUSTSEC-2025-0009 --ignore RUSTSEC-2025-0055 --ignore RUSTSEC-2023-0091 --ignore RUSTSEC-2024-0438 --ignore RUSTSEC-2025-0118 --ignore RUSTSEC-2026-0020 --ignore RUSTSEC-2026-0021 --ignore RUSTSEC-2025-0141 --ignore RUSTSEC-2024-0388 --ignore RUSTSEC-2025-0057 --ignore RUSTSEC-2024-0384 --ignore RUSTSEC-2020-0168 --ignore RUSTSEC-2022-0061 --ignore RUSTSEC-2024-0436 --ignore RUSTSEC-2024-0370 --ignore RUSTSEC-2025-0010 --ignore RUSTSEC-2021-0127 --ignore RUSTSEC-2026-0002 --ignore RUSTSEC-2024-0442`; `cargo clippy -p myosu-chain-runtime --features fast-runtime -- -D warnings`; `cargo clippy -p pallet-game-solver -- -D warnings`; `cargo clippy -p myosu-chain --features fast-runtime -- -D warnings`; `cargo check -p myosu-chain --features fast-runtime`; `cargo test -p pallet-game-solver --quiet -- stage_0`. Commit: `5a12551d44f61f4eb20089323de53303c6a9db3d`.
- 2026-04-05 `P-006` Audited the live stage-0 `pallet-game-solver` dispatch/storage surface, recorded the decision document at `gen-20260405-145446/p-006-stage0-surface-audit.md`, removed the finished queue entry, and re-scoped the later extrinsic-reduction task around the audit result that the active default-build `SubtensorModule` surface is already the enforced 8-call set. Validation: `cargo test -p pallet-game-solver stage_0_flow_dispatch_surface_matches_live_chain_loop --quiet`; `cargo test -p pallet-game-solver stage_0_flow_registers_stakes_serves_and_emits --quiet`; `cargo test -p pallet-game-solver --quiet -- stage_0`; `git diff --check`. Commit: `f92abef66894102dfde1216078aab6ad1f021984`.
- 2026-04-05 `P-007` Added a default-build sparse/dense epoch parity proof in `src/tests/determinism.rs`, keeping the legacy dense epoch compatibility path while proving it produces the same epoch output and persisted state vectors as the sparse production path under identical seeded inputs. Validation: `cargo test -p pallet-game-solver dense_sparse_epoch_paths_produce_identical_state --quiet`; `cargo test -p pallet-game-solver determinism_identical_yuma_inputs_produce_stable_epoch_outputs --quiet`; `cargo test -p pallet-game-solver --quiet`; `cargo clippy -p pallet-game-solver --all-targets -- -D warnings`. Commit: `d0b7b10ac168226e038f65c604df2e71a9c04a6e`.
- 2026-04-05 `P-008` Added `fuzz`-filtered property coverage for poker and Liar's Dice wire codecs: query/response roundtrip proofs, arbitrary-byte decode no-panic guards, and the missing `proptest` dev dependency for Liar's Dice. Also fixed the existing `io::Error::other` clippy blocker in poker test code so the touched crates could pass the repo warning bar truthfully. Validation: `cargo test -p myosu-games-poker -- fuzz`; `cargo test -p myosu-games-liars-dice -- fuzz`; `cargo test --quiet -p myosu-games -p myosu-games-poker -p myosu-games-liars-dice -p myosu-play -p myosu-miner -p myosu-validator`; `cargo clippy -p myosu-games-poker -p myosu-games-liars-dice --all-targets -- -D warnings`. Commit: `11f6662f8d390e2ce36f1cf4cf261cca24865716`.
