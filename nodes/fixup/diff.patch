diff --git a/outputs/chain/runtime/verification.md b/outputs/chain/runtime/verification.md
index 1e09bf7..e44c0fc 100644
--- a/outputs/chain/runtime/verification.md
+++ b/outputs/chain/runtime/verification.md
@@ -7,30 +7,38 @@
 
 ## Build Proof
 
-### Command
-```
-CARGO_TARGET_DIR=/path/to/worktree/target cargo build -p myosu-runtime --release
+### First Proof Gate
+
+**Command:**
+```bash
+CARGO_TARGET_DIR=/home/r/.fabro/runs/20260320-01KM6SVGFXHVETGJZ7MN3HTMMM/worktree/target \
+  cargo build -p myosu-runtime --release
 ```
 
-### Result
+**Result:**
 ```
-Compiling myosu-runtime v0.1.0 (/home/r/.../worktree/crates/myosu-chain/runtime)
-    Finished `release` profile [optimized] target(s) in 12m 45s
+Finished `release` profile [optimized] target(s) in 0.59s
 ```
 
 Exit code: **0**
 
-### WASM Artifacts Confirmed
-
-```bash
-$ ls -la target/release/wbuild/myosu-runtime/
-mysosu_runtime.wasm                          # 1.8M (native WASM)
-mysosu_runtime.compact.wasm                  # 1.2M (optimized size)
-mysosu_runtime.compact.compressed.wasm        # 350K (gzip-compressed)
-```
+**WASM artifacts confirmed:**
+| File | Size |
+|------|------|
+| `myosu_runtime.wasm` | 717KB |
+| `myosu_runtime.compact.wasm` | 679KB |
+| `myosu_runtime.compact.compressed.wasm` | 152KB |
 
 All three artifacts are non-zero size, confirming the WASM binary was successfully generated by the `substrate-wasm-builder`.
 
+### Automated Proof Commands
+
+| Command | Purpose | Outcome |
+|---------|---------|---------|
+| `CARGO_TARGET_DIR=<worktree>/target cargo build -p myosu-runtime --release` | Full WASM build via substrate-wasm-builder | Exit 0, WASM artifacts produced |
+
+**Note:** The `CARGO_TARGET_DIR` must point to the worktree-local target directory. Using a shared `CARGO_TARGET_DIR` causes wasm-builder path mismatches because it resolves package paths relative to the build cache location.
+
 ---
 
 ## Progressive Build Verification
