You are running the `malina gen` implementation-planning pass for repository `/home/r/Coding/myosu`.

Study these inputs first:
- Generated specs: `gen-20260402-120305/specs`
- Current codebase reality: `/home/r/Coding/myosu-gen`
- Copied planning corpus with reviewed intent and requirement context: `gen-20260402-120305/corpus`
- Existing implementation plan path to overwrite: `gen-20260402-120305/IMPLEMENTATION_PLAN.md`

Use up to 5 parallel subagents when helpful for repo reads and spec-vs-code comparison.
Use those subagents aggressively for code search, entry-point tracing, test discovery, and spec-vs-code gap analysis, but keep final synthesis centralized in this main Opus pass.

Goal:
- Author a real Ralph-style implementation plan derived from the corpus-first generated specs and grounded in current code reality.
- This is not a readiness report, classification report, or provenance report.
- Do not implement anything in this pass.
- Shape the plan so the live runner can keep up to 5 implementation lanes busy when independent work exists.

Rules:
- Compare `specs/*.md` against the actual code before declaring work missing.
- Every plan item must be grounded in concrete codebase evidence or a verified gap.
- Required tests belong in this plan, derived from acceptance criteria.
- Make the plan read like a concrete execution queue, not like a heuristic summary or retrospective.
- Every unchecked item must be small enough for one Ralph task loop: implement -> validate -> review -> commit.
- Do not emit broad epics. Split multi-stage work into sequenced execution slices.
- Each executable item must have one narrow outcome. If the title or scope needs `and`, split it.
- If the target repo is greenfield or mostly unimplemented, you must emit an explicit early bootstrap / extraction / adaptation tranche before later feature work. Those tasks should create the minimum local workspace, crate, module, and wiring surfaces needed to make the later spec work real.
- If the specs rely on upstream codebases or reference implementations, convert that into concrete extraction / adaptation tasks with narrow owned surfaces. Do not hide the migration work inside vague feature tasks.
- Prefer a dependency graph with as much safe parallelism as the codebase permits. Do not serialize unrelated work just because it lives under the same broad feature area.
- Each executable item must name concrete owned surfaces so the runner can enforce scope.
- Each executable item must name concrete integration touchpoints in the existing codebase that the change must wire into.
- Do not create island tasks. New files, crates, or modules are only valid when the plan item also wires them into existing entry points, registries, routes, dispatch tables, configs, tests, or exports.
- At least one required test should observe the integration path, not just the existence of newly created files, unless the task is a true bootstrap exception already grounded in existing repo surfaces.
- Do not include source classification, generated readiness, planning-context labels, repo-inspection tasks, or planning-only bootstrap/admin tasks.
- Do not include lane labels, phase headings, dependency-graph appendices, fenced code blocks, or any prose scheduling summary outside the task entries themselves.
- The only scheduling surface is per-task `Dependencies`. Do not describe work as `Lane 1`, `Lane 2`, `independent lane`, or similar.
- If a spec is already fully satisfied by the codebase, do not create a fake task for it.
- Keep the plan actionable and prioritized. Higher priority items should unblock the most system value.
- Do not rename any required field labels. Use `Why now`, never `Why later`. Use `Dependencies`, never `Depends on`. Use `Scope boundary`, never `Scope`.
- `## Priority Work` and `## Follow-On Work` may contain only task entries. Do not insert any `###` headings or descriptive sub-sections.
- For Rust bootstrap tasks, default to the latest stable toolchain currently installed here: `rustc 1.94.1 (e408947bf 2026-03-25)
` and `cargo 1.94.1 (29ea6fb6a 2026-03-24)
`.
- For new Rust workspace / crate manifests, prefer sensible modern defaults unless the codebase evidence says otherwise:
  - `edition = "2024"`
  - `resolver = "3"` at the workspace level
  - `rust-version` aligned with the current stable toolchain
  - canonical workspace members and package names that match the local repo, not stale upstream names
- Do not claim `edition = "2024"` is invalid. It is valid here.
- Treat the generated specs as the target contract. Use the actual codebase to determine what is already satisfied, what can be reused, and what still needs implementation.
- The copied planning corpus is requirement context, not optional flavor text. Use it to preserve reviewed priorities and to resolve ambiguity in favor of the reviewed genesis.
- Empty queue is only valid if the generated specs are actually satisfied by the current codebase. Do not downgrade missing corpus-defined behavior into a retrospective status report.
- Your stdout response must be minimal. Do not emit insight callouts, summaries, bullets, or commentary about the plan. Write the file, then reply with exactly `IMPLEMENTATION_PLAN_WRITTEN`.

Write exactly one markdown file at `gen-20260402-120305/IMPLEMENTATION_PLAN.md` using this shape:

# IMPLEMENTATION_PLAN

Short introductory paragraph explaining that this is the prioritized implementation queue derived from the specs and current code review.

## Priority Work

For each incomplete item, use a task entry like:
- [ ] `TASK-ID` Concise outcome-focused title
  - Spec: `specs/NNN-topic.md`
  - Why now: ...
  - Codebase evidence:
    - ...
  - Owns:
    - `path/to/file`
    - `path/to/dir/`
  - Integration touchpoints:
    - `existing/path/or/surface`
  - Scope boundary:
    - ...
  - Required tests:
    - `command`
    - `command`
  - Dependencies: `OTHER-001`, `OTHER-002` or `none`
  - Completion signal: ...

## Follow-On Work

Use this only for lower-priority work that should wait until the priority queue advances.
Every item in this section must use the exact same task-entry shape as `## Priority Work`.
That means every follow-on item still needs `Spec`, `Why now`, `Codebase evidence`, `Owns`, `Integration touchpoints`, `Scope boundary`, `Required tests`, `Dependencies`, and `Completion signal`.

## Completed / Already Satisfied

Optional section for specs or major items already proven complete in the current codebase.
Do not put incomplete work here.
Do not add any additional headings or appendices after this section.

Keep dependencies honest:
- Only name a dependency when the task truly cannot land before that other item.
- Prefer direct task dependencies, not vague spec-level dependencies.
- If two tasks can proceed independently in separate worktrees, do not make one depend on the other.

Current generated spec inventory:
- gen-20260402-120305/specs/001-chain-runtime-reduction.md
- gen-20260402-120305/specs/002-single-token-emission-accounting.md
- gen-20260402-120305/specs/003-unified-observability.md
- gen-20260402-120305/specs/004-security-audit-process.md
- gen-20260402-120305/specs/005-integration-test-harness.md
- gen-20260402-120305/specs/006-multi-node-devnet.md
- gen-20260402-120305/specs/007-operator-onboarding.md
- gen-20260402-120305/specs/008-release-governance.md
- gen-20260402-120305/specs/009-third-game-extensibility-proof.md
- gen-20260402-120305/specs/010-python-research-quality-gates.md
- gen-20260402-120305/specs/011-architecture-decision-records.md

Before finishing:
- Re-read the plan and remove any item that is not grounded in the actual code or an actual spec gap.
- Make sure each actionable item includes required tests.
- Make sure each actionable item includes owned surfaces.
- Make sure each actionable item includes integration touchpoints into existing repo surfaces.
- If the repo is greenfield, make sure the first priority items explicitly cover bootstrap / extraction / adaptation work rather than assuming those surfaces already exist.
- If upstream repos are materially relevant, make sure the plan says which local files/modules are being extracted or adapted first, in small slices, instead of burying that in generic feature prose.
- Make sure every task, in every incomplete-work section, uses `Spec`, `Why now`, `Codebase evidence`, `Owns`, `Integration touchpoints`, `Scope boundary`, `Required tests`, `Dependencies`, and `Completion signal`.
- Every `Spec` value must be exactly one existing path under `specs/*.md`. Do not write `all specs`, comma-separated paths, or prose there.
- Remove any `###` headings, lane labels, dependency-graph summaries, or fenced code blocks before finishing.
- Make sure the plan reads like a concrete execution queue, not like a heuristic summary.
- Make sure the final assistant response is exactly `IMPLEMENTATION_PLAN_WRITTEN`.

Now write `gen-20260402-120305/IMPLEMENTATION_PLAN.md`.